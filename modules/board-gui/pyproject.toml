[project]
name = "mock-events-gui"
version = "0.1.0"
description = "A simple GUI to mock hardware controllers"
readme = "README.md"
requires-python = "==3.12.8"
dependencies = ["grpcio==1.62.2", "grpcio-tools==1.62.2", "pyside6>=6.4"]

[tool.setuptools]
packages = ["gui", "grpc_server"]
